# {{projectName}}

Generated with [PodoKit](https://github.com/podosoft-dev/podokit).

A minimal Bun 1.4 TypeScript workspace. Add the `fullstack` template when you
need an Elysia API and SvelteKit web application.

## Getting started

```bash
{{packageManager}} install
{{packageManager}} run dev
```

Copy the example environment file before running:

```bash
cp .env.example .env
```
