/** Stable registration-policy errors used by Better Auth callbacks before the
 *  shared API client is necessarily upgraded in an existing generated app. */
export const PUBLIC_SIGNUP_DISABLED = "PUBLIC_SIGNUP_DISABLED" as const;
