{
  "name": "api-key-auth",
  "description": "API-key auth for machine/service clients (X-API-Key), separate from user sessions. Provides an Elysia guard helper and shared constant-time ApiKeyVerifier. Requires auth.",
  "requires": ["auth"],
  "targetApp": "api",
  "env": [
    "# api keys for machine clients (comma-separated)",
    "API_KEYS=dev-key-please-change"
  ],
  "inject": [
    { "file": "apps/api/src/app.ts", "marker": "// podokit:end:imports", "text": "import { apiKeyModule } from \"./api-key/api-key.module\";" },
    { "file": "apps/api/src/app.ts", "marker": "// podokit:end:modules", "text": "apiKeyModule," }
  ],
  "instructions": [
    "Set API_KEYS in .env (comma-separated).",
    "Protect a route for machine clients with requireApiKey(request.headers, verifier).",
    "Call it:  curl -H 'x-api-key: dev-key-please-change' localhost:5002/machine/ping"
  ]
}
