{
  "name": "{{projectName}}-tests",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test:e2e": "bunx --bun playwright test",
    "test:e2e:ui": "bunx --bun playwright test --project=ui",
    "test:e2e:api": "bunx --bun playwright test --project=api",
    "test:e2e:report": "bunx --bun playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "otpauth": "^9.5.1"
  }
}
