---
"@podosoft/podokit": minor
---

Support versioned external package modules during `podo update`, add explicit managed-path adoption with `podo add --adopt`, and preserve pre-existing application drift and unrelated files when refreshing the generated lock.
