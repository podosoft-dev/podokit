---
"@podosoft/podokit": patch
---

Route exact WebSocket paths to the API in Docker Compose deployments without exposing a second host port, and trust forwarded headers only from explicitly configured proxy CIDRs.
