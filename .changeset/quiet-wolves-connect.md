---
"@podosoft/podokit": patch
---

Add a detached `podo dev up` lifecycle command, restore shared gateway routing when refreshing generated development apps, remove stale routes when a project's local hostname changes, and safely deliver new or pristine owned seeds without overwriting application edits, deletions, or moves.
