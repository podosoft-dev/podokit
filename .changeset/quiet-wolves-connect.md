---
"@podosoft/podokit": patch
---

Add a detached `podo dev up` lifecycle command, restore shared gateway routing when refreshing generated development apps, and remove stale routes when a project's local hostname changes.
