{
  "name": "@podosoft/podokit-monorepo",
  "version": "0.9.0",
  "private": true,
  "description": "PodoKit monorepo — starter toolkit and CLI for full-stack TypeScript apps.",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/podosoft-dev/podokit.git"
  },
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @podosoft/podokit-contracts && npm run build -w @podosoft/podokit-auth && npm run build -w @podosoft/podokit-template-engine && npm run build -w @podosoft/podokit-api-client && npm run build -w @podosoft/podokit && npm run build -w @podosoft/podokit-mcp",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test:scripts && npm run test --workspaces --if-present",
    "test:scripts": "node --test scripts/*.test.mjs",
    "e2e": "node scripts/e2e-ci.mjs",
    "e2e:smoke": "node scripts/e2e-ci.mjs --smoke",
    "e2e:package-smoke": "node scripts/e2e-ci.mjs --package-smoke",
    "changeset": "changeset",
    "version": "changeset version && node scripts/sync-template-dependency-ranges.mjs && npm install --package-lock-only"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^1.0.0",
    "@changesets/cli": "^3.0.0",
    "@types/node": "^20.17.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
