{
  "name": "@podosoft/podokit-monorepo",
  "version": "0.3.0",
  "private": true,
  "description": "PodoKit monorepo \u2014 starter toolkit and CLI for full-stack TypeScript apps.",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/podosoft-dev/podokit.git"
  },
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @podosoft/podokit-template-engine && npm run build -w @podosoft/podokit-api-client && npm run build -w @podosoft/podokit",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "e2e": "node scripts/e2e-ci.mjs",
    "e2e:smoke": "node scripts/e2e-ci.mjs --smoke"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
