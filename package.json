{
  "name": "@podosoft/podokit-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "PodoKit monorepo — starter toolkit and CLI for full-stack TypeScript apps.",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/podosoft-dev/podokit.git"
  },
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "echo \"no packages yet\" && exit 0",
    "lint": "echo \"no packages yet\" && exit 0",
    "test": "echo \"no packages yet\" && exit 0"
  }
}
