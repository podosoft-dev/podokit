{
  "name": "@podosoft/podokit-monorepo",
  "version": "0.2.0",
  "private": true,
  "description": "PodoKit monorepo \u2014 starter toolkit and CLI for full-stack TypeScript apps.",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/podosoft-dev/podokit.git"
  },
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @podosoft/podokit-template-engine && npm run build -w @podosoft/podokit",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "typescript": "^5.6.3",
    "vitest": "^4.1.9"
  }
}
